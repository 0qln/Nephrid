[package]
name = "nephrid"
version = "26.5.10+6d100b3"
edition = "2024"

[features]

nn-backend-cuda = []
nn-backend-ndarray = []

mcts-pure = []
mcts-nn = []
mcts-hce = []

default = ["mcts-hce"]

[dependencies]
engine = { version = "*", path = "../engine", default-features = false }
regex = "1.12.3"

[dev-dependencies]
assert_cmd = "2.0"
ntest = "0.9.5"

[lints]
workspace = true
