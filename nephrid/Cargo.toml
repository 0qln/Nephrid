[package]
name = "nephrid"
version = "26.3.10+7d0e2f2"
edition = "2024"

[features]
cpu = ["engine/nn-backend-ndarray"]
gpu = ["engine/nn-backend-cuda"]
mcts-nn = ["engine/mcts-nn"]
mcts-sa = ["engine/mcts-sa"]
mcts-pure = ["engine/mcts-pure"]

default = ["cpu", "mcts-sa"]

[dependencies]
engine = { version = "*", path = "../engine", default-features = false }
