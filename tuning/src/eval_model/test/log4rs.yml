appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%H:%M:%S)} {l} {t} - {m}{n}"

  file:
    kind: file
    path: "out/eval_model/test/main.log"
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S)} {l} {t} - {m}{n}"

root:
  level: debug
  appenders:
    - file

loggers:
  train:
    level: info
    appenders:
      - stdout
      - file
    additive: false

  test:
    level: debug
    appenders:
      - stdout
      - file
    additive: false

  data:
    level: debug
    appenders:
      - file
    additive: false
